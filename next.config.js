/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['miro.medium.com', 'source.unsplash.com', 'postify-bucket.s3.us-east-2.amazonaws.com']
    }
}

module.exports = nextConfig
